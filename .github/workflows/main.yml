name: CI/CD

on:
  push:
    branches: [ master ]

jobs:
  build:
    name: Building project and running tests
    runs-on: ubuntu-latest

    steps:
    - name: Checking-out repository
      uses: actions/checkout@v2
    - name: Using Node.js
      uses: actions/setup-node@v1
    - name: Installing dependencies
      run: npm ci
    - name: Building project
      run: npm run build
    - name: Running tests
      run: npm run test
  
  deploy:
    needs: [build]
    name: Deploying to production servers
    runs-on: ubuntu-latest
    
    steps:
    - name: Copy files to La Herradura's remote server
      uses: garygrossgarten/github-action-scp@release
      
      with:
        local: /home/runner/work/vehiclesdealer-backend/vehiclesdealer-backend/dist/main.js
        remote: root/new-backend.js
        host: 185.166.215.197
        username: root
        password: ykBsL4Bccw4Hivoj

